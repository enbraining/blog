---

type Props = {
  title: string;
};

const { props, url } = Astro;
const { title } = props;
---

<div class="mb-7">
  <div class="flex items-center">
    <h1 class="-mb-0.5 font-semibold leading-9 text-heading">{title}</h1>
    <div class="ml-auto flex gap-1">
    </div>
  </div>
  <slot />
</div>
